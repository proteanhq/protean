Deprecated the `debug` argument on `Supervisor` and `Reloader`: `debug=True` now emits a `RemovedInProtean020Warning` and the argument is removed in 0.20.0. Pass `log_level="DEBUG"` instead. Both classes also take `log_format` and `log_config`, matching the CLI's logging flags.

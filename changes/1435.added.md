`protean ir diff --format=event-model` renders the diff in EventModeling slice vocabulary. An added command is a new slice, a removed projection is a removed read model, a projection that gained a field is a changed slice, over the same snapshot loaders and exit codes as the text format.

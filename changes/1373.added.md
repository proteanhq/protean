Added a machine-checked TLA+ specification of the aggregate optimistic-concurrency no-lost-update protocol (`specs/OCC.tla`), joining the existing checkpoint and outbox specs. Design-time verification, not a CI gate.

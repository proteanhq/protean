Added a machine-checked TLA+ specification of the aggregate optimistic-concurrency no-lost-update protocol, joining the existing checkpoint and outbox specs under `specs/`. Design-time verification, not a CI gate.

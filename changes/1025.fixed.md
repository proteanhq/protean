Fix per-field `validators=[...]` running against `None` on optional fields. An optional field with a format validator (e.g. `String(validators=[RegexValidator(...)])`) raised a spurious error when simply left unset, because the validator ran on `None`. Empty values now short-circuit per-field validators, matching the legacy field system and the documented execution order (empty → choices → cast → validators).

`mypy` now runs against `src/protean` in CI, gating the source against new type errors. Pre-existing type debt is captured in an append-only-shrinking module quarantine in `[tool.mypy]`, and `warn_unused_ignores` is enabled. See ADR-0018 for the type-safety adoption strategy.

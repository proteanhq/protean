Structured logging baseline: bridge stdlib loggers through the structlog pipeline via `ProcessorFormatter`, fix stack-trace-lost sites by converting `logger.error(str(exc))` to `logger.exception(...)`, convert ~50 hot-path log calls to structured keyword-argument events, and upgrade operationally critical events (engine lifecycle, subscription start/stop, outbox batch completion) from DEBUG to INFO.

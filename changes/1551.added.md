Added a pack-integrity guard (`tests/dx/test_pack_integrity.py`) that walks the DX teaching pack and fails on a dangling internal reference, an asset or reference page no `SKILL.md` reaches, or a skill that does not link the shared verify step. Every skill now links one shared `references/verify-with-check.md`, which states the verify step once: run `protean check` (or the MCP `check` tool) against the changed domain and resolve what it reports. The bar every teaching skill is held to is written up in `references/hardening-bar.md`, linked from the pack's `AGENTS.md`. (#1551)

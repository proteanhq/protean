Emit connection pool and backpressure OTEL metrics: `BaseProvider.pool_stats()` with SQLAlchemy implementation, DB pool gauges (`protean.db.pool_size`, `protean.db.pool_checked_out`, `protean.db.pool_overflow`, `protean.db.pool_checked_in`), Redis broker pool gauge (`protean.broker.pool_active_connections`), and renamed backpressure metrics (`protean.outbox.pending_count`, `protean.subscription.consumer_lag`, `protean.subscription.pending_messages`). Refactored shared metric collection between Observatory and OTEL gauges.

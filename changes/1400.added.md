ADR-0040 writes down the schema-evolution ladder: which mechanism handles which change to an event (weak schema for additive and rename changes, versioning plus an upcaster for structural ones, an operator-level migration for a long chain), and records that lenient mode is a permanent, off-by-default reader.

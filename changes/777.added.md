Five handler-completeness and flow-fitness diagnostics now surface in `protean check`: `QUERY_HANDLER_WITHOUT_QUERY` and `PROJECTOR_HANDLES_ORPHANED_EVENT` and `COMMAND_HANDLER_CROSS_CLUSTER` (warnings), plus `SUBSCRIBER_NO_STREAMS` and `PROCESS_MANAGER_UNCLOSED` (info). Each flags a wiring gap or boundary violation in a projection, projector, command handler, subscriber, or process manager, and honours the existing `suppress_checks` / `[lint.suppressions]` channels.

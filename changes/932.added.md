Publish ADR-0010 (`docs/adr/0010-logging-overhaul-and-wide-event-architecture.md`) capturing the full architectural story of the 6.6 Logging Overhaul epic — the structlog pipeline bridge, wide event access log, `bind_event_context()` API, two-layer HTTP + domain wide events, opt-in tail sampling, auto-configuration on `Domain.init()`, log↔trace correlation, pipeline-level redaction, multi-worker log queue, and the `protean.security` channel — alongside the alternatives considered and explicitly rejected. Includes an index table of current ADRs in the `docs/adr/` README.

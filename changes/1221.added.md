Add name and symbol resolution to the `protean check` source-analysis layer, turning a use-site name in a module's syntax tree into the fully-qualified name it refers to (alias- and relative-import-aware, local definitions shadowing imports) and classifying a resolved name as framework or domain-element. It never guesses: an ambiguous or undeterminable use site is left unresolved rather than bound to a wrong name. Internal substrate for upcoming behavioral rules; today's diagnostics are unchanged.

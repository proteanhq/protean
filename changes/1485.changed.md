Rewrote the docs quickstart to walk through the canonical `Post` domain from `examples/reference_app/blog.py`, pulling every snippet with `--8<--` from the reference module so the page shows the same code the tests run. The walkthrough now closes the write-then-read arc: it adds the `PublishedPostsFeed` projection and a step that queries the feed and prints the published post. The old `docs_src/guides/getting-started/quickstart.py`, which no test ran, is removed.

Add an element and method index to the `protean check` source-analysis layer, mapping a domain's classes and methods to their syntax nodes and tagging each method with the role its element type and decorators imply. Internal substrate for upcoming behavioral rules; today's diagnostics are unchanged.

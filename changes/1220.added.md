Internal: the `protean check` source-analysis layer gained an element and method index — every class in a domain package mapped to its syntax node, every method to its function node, and a role tag (command-handler method, event-handler method, projector on-event, event `apply`, aggregate behavior, repository method) where the element's registered type and the method's decorators say what it is. Substrate for the upcoming body-level rules; the diagnostics you see today are unchanged.

Add intra-procedural dataflow to the `protean check` source-analysis layer, answering three questions inside a single method body: the document order of its statements, the reaching definitions of a local-name use (which assignment(s) in the same body could have bound it), and the `with` context managers and `for`/`while` loops that lexically enclose a statement. It is a may-reach analysis that never guesses: a name assigned in both arms of an `if` resolves to the set of both, a free name to the empty set, and an unresolvable `with` context to `None`. Internal substrate for upcoming behavioral rules; today's diagnostics are unchanged.

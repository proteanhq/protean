A standalone (non-UnitOfWork) SQLAlchemy update that lost the version race raised a raw `StaleDataError`. It now surfaces as `ExpectedVersionError`, the same optimistic-concurrency error the UnitOfWork path and the read/count/flush paths already raise.

A handler method invocation that reaches no repository now carries a stated guarantee that it opens no transaction and holds no pooled connection, so a handler can talk to an external system by putting the call in its own method. Regression tests cover the memory and SQLite providers.

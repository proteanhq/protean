`protean check` reports `HANDLER_PERSISTS_AND_CALLS_OUT` when one handler method both persists through a repository and calls an external system, so the call can hold the transaction open across the call. It is advisory, because a handler that needs the call's result to compute its write is expected to do both in one method; silence it on that handler with `suppress_checks`.

Add a `Decimal` field type for fixed-precision values such as money, backed by Python `decimal.Decimal`. Optional `precision`/`scale` map to `NUMERIC(precision, scale)` on SQL providers and are validated by Pydantic; values are string-encoded in JSON/event payloads so they never round-trip through a binary float and lose precision. Use it instead of `Float` for money.

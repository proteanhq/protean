Add `protean docs generate --type=llms`, which emits a versioned `llms.txt` context pack: a framework layer stamped with the Protean version plus core-documentation links, and, when a domain or IR is given, a project overlay listing the project's own aggregates, commands, events, handlers, and projections.

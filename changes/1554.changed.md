Hardened the DX-pack workflow, refactor, and analysis skills and wired them to the diagnostic contract. Six of them now declare, in their `SKILL.md` frontmatter, the `check` codes they teach the fix for: `add-field` (`UNBOUNDED_INDEXED_STRING`, `UNINDEXED_FILTER_PATH`), `add-validation` (`AGGREGATE_NO_INVARIANTS`, `VALUE_OBJECT_INVARIANT_FAILED`), `add-read-model` (`PROJECTION_WITHOUT_PROJECTOR`, `UNSOURCED_PROJECTION_FIELD`), `refactor-extract-value-object` (`VALUE_OBJECT_MUTABLE_FIELD`), `refactor-introduce-events` (`EVENT_WITHOUT_DATA`, `UNRAISED_EVENT`), and `refactor-move-logic-to-aggregate` (`AGGREGATE_NO_INVARIANTS`). A diagnostic for one of these codes now names the skill that explains the fix through its `teaching_skills`. Also corrected a drifted upcaster example in the `refactor-extract-value-object` migration guide (`event_type`/`from_version`/`to_version`, on a `BaseUpcaster` subclass). (#1554)

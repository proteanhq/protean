Fix the scaffold's `.env.example`, `README.md`, and `docker-compose*.yml` to set `PROTEAN_LOG_LEVEL` instead of the unread `LOG_LEVEL`/`LOG_FORMAT`, so the documented env override actually takes effect.

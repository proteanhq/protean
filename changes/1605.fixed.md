The value-object skill's seven example files now run as their `Usage:` line says (`python <file>`). The line used to import from `protean_skills`, a package that does not exist. Separately, six of the demos failed when run because they never set up their domain, and two of them also had a bug in the demo code.

The DX pack's add-field and add-validation skills now state that `sanitize` is off by default for `String` and `Text` fields. Turn it on per field, or domain-wide with `[field_defaults] sanitize`.
They also say that sanitizing escapes unsafe HTML with `bleach.clean()`. It does not strip it.

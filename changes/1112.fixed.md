Corrected the read-side documentation. The "three levels of read access" now reads the same on both the query-handlers and projections guides (Pipeline `domain.dispatch(query)`, Facade `domain.view_for(Projection)`, Raw `domain.connection_for(Projection)`), with `domain.repository_for(Projection)` called out as the projector write path rather than a read level. Removed the stale claim that `update_all()`/`delete_all()` are blocked on `ReadOnlyQuerySet` (those methods no longer exist on the public QuerySet). Documented the top-level `Q` query-criteria primitive on the QuerySet API page.

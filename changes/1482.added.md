Added a Security Considerations reference page stating Protean's security posture. Field validation is structural and not an injection boundary. `String` and `Text` fields do not sanitize by default; opt in per field with `sanitize=True` or domain-wide with `[field_defaults] sanitize`, and a field with `choices` is never sanitized. The bundled adapters do not build queries by concatenating your field values into query text. Raw queries are the operator's boundary: the provider's `raw(query, data)` binds `data` on SQLAlchemy, while the repository path `QuerySet.raw(query, data)` ignores `data` and runs your string as is. Secrets in configuration are the operator's to supply and protect. Transport security, authentication, rate limiting, and output escaping are left to the operator. Three network services ship, the engine health server (on by default, with a state-changing `POST /drainz`), the Observatory, and `protean mcp --http`; all three are unauthenticated and bind loopback by default.

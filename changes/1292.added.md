`protean upgrade-check` now reports two source-level findings for the 0.17 transaction change: `NESTED_UNIT_OF_WORK` for a Unit of Work opened inside another, and `IO_INSIDE_UNIT_OF_WORK` for HTTP calls, broker publishes and email sends inside one. Both are read-only and advisory.

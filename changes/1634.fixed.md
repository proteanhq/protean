`DomainFixture.domain_context()` now resets the data of its own domain on exit. Before, it reset whichever domain was current at exit, so a test that left another domain's context pushed wiped that other domain and left the fixture's data in place. The fixture also pops its context when a reset raises, so a failing reset no longer leaves the context on the stack for later tests.

Trace validation for the `$all` gap-safe checkpoint: `make verify-specs` now records the real subscription's cursor advance as `_gap_safe_batch` runs an out-of-order commit interleaving and checks the log against `specs/Checkpoint.tla` with TLC, so the shipped code is shown to conform to the model (the settle-then-process low-watermark from #1088), not just that the model is sound. Two negative checks keep a green run honest: a seeded gap-skip log (advancing past a still-open gap) is rejected as a divergence, and an in-order log with no gap is rejected as under-covered. Joins the OCC trace validation from #1382; design-time verification, not a CI gate.

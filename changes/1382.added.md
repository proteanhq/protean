Trace validation for the OCC protocol: `make verify-specs` now records the real Memory and SQLAlchemy compare-and-set as it runs and checks the log against `specs/OCC.tla` with TLC, so the shipped code is shown to conform to the model, not just that the model is sound. Two negative checks keep a green run honest: a seeded lost-update log is rejected as a divergence, and a log with no conflict is rejected as under-covered.

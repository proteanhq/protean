Fixed three things a 0.17.0 release would otherwise have shipped. `make generate-docker` in a freshly generated project answered `No such command 'generate'`, because the scaffold's Makefile still called a CLI group this release removed; the generated README also pointed at `make test-unit` and `make test-bdd`, neither of which the generated Makefile defines. The version-bump config searched for a bare `0.16.0` with no anchor, so cutting a release rewrote `"typer>=0.16.0"` to `"typer>=0.17.0"` and ratcheted a dependency floor once per release. And the front-page README linked to a contributing guide with no index page.

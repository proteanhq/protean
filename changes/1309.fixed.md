Fixed four things a 0.17.0 release would otherwise have shipped:

- `make generate-docker` in a generated project answered `No such command 'generate'` — the scaffold's Makefile called a CLI group this release removed.
- The generated README pointed at `make test-unit` and `make test-bdd`, neither of which the generated Makefile defines.
- Cutting a release rewrote `"typer>=0.16.0"` to `"typer>=0.17.0"`, because the version-bump config searched for a bare version string with no anchor.
- The front-page README linked to a contributing guide with no index page.

Added a differential adapter-parity test harness: it replays the same randomized history of repository operations against two adapters (Memory vs SQLite in the core suite, Memory vs PostgreSQL in the full suite) and fails if their observable outcomes (final state, version, query results, or raised exception class) diverge. It catches adapter-specific divergences that single-adapter property tests and fixed conformance tests cannot.

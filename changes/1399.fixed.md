Redis' `remove_by_key_pattern` no longer raises when a pattern matches no keys. It used to call `delete()` with zero arguments in that case, which Redis rejects; it now skips the delete, matching the in-memory adapter's silent no-op.

`@domain.repository(database="mssql")` and `database="mysql"` are now accepted. The `Database` enum that validates a repository's `database=` binding listed only elasticsearch, memory, postgresql and sqlite, so binding a repository to a registered, working MSSQL provider was rejected with "should be associated with a valid Database". (#1580)

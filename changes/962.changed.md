Raise the minimum Elasticsearch client to `elasticsearch>=7.17.9` (with a matching `elasticsearch-dsl`), which supports `urllib3` 2.x and resolves the related `urllib3` security advisory. The Elasticsearch adapter is validated against the 7.17.x client.

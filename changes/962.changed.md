Upgrade the Elasticsearch adapter to the version 8 client (`elasticsearch` / `elasticsearch-dsl` 8.x). This resolves a `urllib3` security vulnerability that the version 7 client's dependency pins were holding back. Projects using the `elasticsearch` extra should run against an Elasticsearch 8.x-compatible server.

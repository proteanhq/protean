Loading an event-sourced aggregate no longer fails when its snapshot predates the current schema. A snapshot that no longer matches the aggregate (a field renamed, removed, or newly required) is discarded, the aggregate is rebuilt from its event stream, and a warning is logged on the `protean.snapshot` channel so you know to rebuild snapshots.

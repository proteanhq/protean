Added a property-based (Hypothesis) serialization round-trip test suite under `tests/serialization/`, pinning that `to_dict()` and `Type(**data)` reconstruction are inverses across every serializable field type, nested value objects, and `HasMany`/`HasOne` associations. Regression cases lock the datetime-ISO (#1039), JSON-serializable date (#1046), and all-default value-object (#1078) fixes.

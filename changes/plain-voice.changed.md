Rewrote the documentation in plain language. Removed 3,612 dashes from running prose, replacing each with the punctuation the sentence actually needed, and applied one form for headings, list lead-ins, and section indexes across the site. Fixed the content bugs the pass turned up along the way: a contradictory complexity figure, "5 ports" that were four, a "10-part" tutorial with 22 chapters, three fitness-function rules missing from the catalog, and the missing MSSQL provider page. `tests/test_diagnostic_codes_are_documented.py` now checks the catalog and the code registry against each other in both directions. Docstrings that the API pages render were swept too, and their Sphinx `:class:`/`:meth:`/`:func:` roles are now real cross-references instead of literal text. Documentation only; no source behaviour or public-API change.

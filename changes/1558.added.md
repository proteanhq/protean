Added `teaching_skills` to the MCP `explain` tool's result: the DX-pack skills that teach the diagnostic code, `[]` when none do. Added a coverage guard over the DX pack so a new lint code that lands with no teaching skill and no documented exemption fails the test suite instead of shipping silently uncovered. (#1558)

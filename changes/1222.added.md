Add a behavioral fact catalog to the `protean check` source-analysis layer, reading a method body and reporting the calls it makes (with the callee's fully-qualified name, the keyword fields it passes, and the Protean role of the receiver: repository query, `raise_`, or Unit of Work), the attributes it reads and writes (`self.total` versus `self.status = ...`), and the domain elements it constructs (`Order(...)`). It stays conservative: a `**kwargs` star or a variable-held receiver is left unresolved rather than guessed, so every verdict is reproducible. Internal substrate for upcoming behavioral rules; today's diagnostics are unchanged.

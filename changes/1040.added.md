Add outbox reconciliation to recover the residual event-store/outbox crash window: `protean outbox reconcile` re-derives missing outbox rows from the event store on demand, and the server runs the same sweep automatically on startup. See ADR-0015.

Added the `UNSOURCED_PROJECTION_FIELD` fitness function. It flags a projection field that no projector write fills, catching a dead read-model column that renders and carries a type but nothing ever populates. The check reads what projector methods write and stays conservative: a `**kwargs` construction, a field filled through a path it cannot follow, and the projection's identity field are all left alone.

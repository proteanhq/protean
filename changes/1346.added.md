Add `protean docs generate --type=agents`, which emits a versioned `AGENTS.md` negative-constraint pack: one hard rule for every error-level diagnostic code, built from the code's meaning, rationale, and fix, telling an agent what not to do. A freshly generated project (`protean new`) now ships this `AGENTS.md` at its root, kept byte-identical to the command's output.

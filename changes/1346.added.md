Add `protean docs generate --type=agents`. It emits a versioned `AGENTS.md` with one hard rule for every error-level diagnostic code, built from that code's meaning, rationale, and fix. `protean new` now writes this `AGENTS.md` at the project root, byte-identical to the command's output.

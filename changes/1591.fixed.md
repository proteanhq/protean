Three DX pack examples declared a value object and never used it. The event-sourced order, the add-field customer and the test-scaffold order now embed `Money` and `ContactInfo` as `ValueObject` fields.

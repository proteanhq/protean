Deprecated the patch-and-persist API: `DAO.update()` and `QuerySet.update()` now emit a `RemovedInProtean10Warning` and are removed in 1.0.0. Load the aggregate, invoke a behaviour method that applies the change, and persist it with `repository.add()`; for a bulk change, run that loop over each match.

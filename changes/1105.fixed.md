Fixed the v0.15 migration guide's Value Object list example, which showed `ValueObjectList(content_type=OrderItem)` with a raw VO class — that raises `ValidationError` because `ValueObjectList` accepts only primitives or a `ValueObject` descriptor. The supported declaration form is `List(content_type=ValueObject(OrderItem))`.

Fixed the v0.15 migration guide's Value Object list example, which showed `List(content_type=OrderItem)` with a raw VO class — that raises `ValidationError`. The supported form is `List(content_type=ValueObject(OrderItem))`.

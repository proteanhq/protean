The DX pack's custom-validator skill now says that a field's validators run in list order and validation stops at the first failing validator. The user sees one error for the field at a time. Empty values skip the validators.

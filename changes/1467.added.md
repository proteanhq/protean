Added a `protean mcp` command that runs a Model Context Protocol server, exposing `validate`, `check`, `introspect`, `explain`, and `scaffold` as tools answered from the installed framework. It runs on stdio by default (`--http` for streamable HTTP) and ships behind the optional `protean[mcp]` extra. The `scaffold` tool previews by default and writes only when called with `apply=true`.

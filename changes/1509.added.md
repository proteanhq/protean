A comparison-eval harness under `tests/eval/` (kept out of the wheel): a context-driven runner that drives an agent over the versioned DX pack to produce a project, and a pack-versioned transcript record/replay format. The replay lane runs in CI with no model access; the live lane is opt-in and records the transcripts. This is the foundation the comparison eval scores on.

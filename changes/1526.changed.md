The schema-evolution guides (`evolving-events.md`, `event-versioning-and-evolution.md`, `event-upcasting.md`) now point at the snapshot caveat: upcasters run on replayed events, not on snapshots. A snapshot holds aggregate state, so when a stored snapshot no longer constructs under the current aggregate schema, Protean discards it and rebuilds the aggregate by replaying its event stream.

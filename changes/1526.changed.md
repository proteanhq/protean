The schema-evolution guides (`evolving-events.md`, `event-versioning-and-evolution.md`, `event-upcasting.md`) now point at the snapshot caveat: upcasters run on replayed events, not on snapshots, so a schema-breaking change discards the stale snapshot and rebuilds by replay.

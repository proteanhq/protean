Fix `protean ir check` falsely reporting a domain as stale after a framework upgrade that only changes the IR schema version. The staleness checksum now covers domain content only (excluding `$schema`, `ir_version`, `generated_at`, and `elements`), matching what `ir diff` compares, so the two commands agree. Baselines materialized before this fix report stale once; regenerate them with `protean ir show` to refresh.

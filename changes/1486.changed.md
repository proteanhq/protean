Replaced the README "Quick Start" block with the canonical `Post` domain from `examples/reference_app/blog.py` (annotation field style, includes the `PublishedPostsFeed` projection), so the README and the reference app now show one Protean. A guard test compares the README block against a marked region of the reference module verbatim and fails with a named drift if they diverge.

Rewrite the logging guide and add a factual reference page plus a conceptual explainer. `docs/guides/server/logging.md` is now a focused task-oriented guide (quick start, `[logging]` config, CLI overrides, `bind_event_context()`, disable auto-configuration). `docs/reference/logging.md` enumerates every `[logging]` key, CLI flag, environment variable, public API, and framework logger — including every event name, level, and field schema for `protean.access`, `protean.perf`, `protean.security`, `protean.server.*`, `protean.core.unit_of_work`, and the broker/repository adapters. `docs/concepts/observability/logging.md` explains the wide event pattern, query-oriented field design, high-cardinality backend selection, the four sources of context, and what Protean deliberately does not do. The correlation-and-causation guide now points at the new pages instead of re-documenting the setup; the new-project scaffold README references `domain.init()` auto-configuration instead of the old `configure_logging()` pattern. Tail-sampling and FastAPI HTTP wide event sections are intentionally deferred until their implementation issues (#924, #925) land — documenting them now would speculate about unmerged behavior.

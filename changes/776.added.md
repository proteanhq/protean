Add two bounded-context fitness functions to `protean check`. `CIRCULAR_CLUSTER_DEPENDENCY` (warning) flags aggregate clusters whose cross-aggregate identity references form a directed cycle, which prevents the aggregates from being decomposed, deployed, or event-sourced independently; the within-aggregate child→root back-pointer and any intra-cluster reference are excluded, so only genuine inter-cluster cycles are reported. `INFRA_IMPORT_IN_DOMAIN` (warning, opt-in via `[lint].check_infra_imports = true`) flags any domain element whose source module imports from `protean.adapters`, coupling the domain layer to a concrete adapter. Both codes are in the `bounded_context` category and honour the `suppress_checks` option, the `[lint].suppressions` allow-list, and the `[lint].level` floor.

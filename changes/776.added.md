Add two bounded-context fitness functions to `protean check`. `CIRCULAR_CLUSTER_DEPENDENCY` (warning) flags every aggregate cluster whose cross-aggregate identity references make it part of a directed cycle — computed by strongly-connected-component membership, so a cluster on more than one cycle is reported exactly once and no cluster in a cyclic component is missed; the within-aggregate child→root back-pointer and any intra-cluster reference are excluded, so only genuine inter-cluster cycles are reported. `INFRA_IMPORT_IN_DOMAIN` (warning, opt-in via `[lint].check_infra_imports = true`) flags any registered domain element — aggregate, entity, value object, repository, handler, domain service, process manager, subscriber, and so on — whose source module has a top-level import from `protean.adapters`, coupling the domain layer to a concrete adapter; imports guarded by `TYPE_CHECKING`, `try`/`except`, or a function body are ignored. Both codes are in the `bounded_context` category and honour the `suppress_checks` option, the `[lint].suppressions` allow-list, and the `[lint].level` floor.

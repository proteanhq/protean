Add an injectable `domain.clock` seam so tests can make time-dependent behavior deterministic. Every domain now exposes `domain.clock` (a `protean.utils.Clock`), defaulting to `SystemClock` (real UTC), and the command-deadline path, retry/lock boundaries on the `Outbox` aggregate, and message timestamps read "now" through it. Assign a stub clock (`domain.clock = my_clock`) to freeze or advance time and prove exact-instant deadline/expiry/backoff semantics without monkeypatching `datetime`. Default behavior is unchanged. The `protean new` example scaffold now generates timezone-aware `datetime.now(UTC)` timestamps instead of naive `datetime.now()`.

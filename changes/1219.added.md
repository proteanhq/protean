Internal: the `protean check` engine gained a shared source-analysis layer that locates, parses and caches a module's syntax tree per build, and can walk a domain package's files directly. It is the substrate the upcoming body-level rules build on; the diagnostics you see today are unchanged.

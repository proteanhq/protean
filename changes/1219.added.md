`protean check` gained a shared source-analysis layer: the check engine now resolves, parses and caches a module's source once per build and hands the same read-only syntax tree to every rule that needs it, instead of each rule parsing files for itself. Source that cannot be located, read or parsed is skipped rather than failing the run. Diagnostics are unchanged; this is the substrate the upcoming body-level rules build on.

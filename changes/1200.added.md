A [Consistency & Delivery Guarantees](reference/guarantees.md) reference page states, per port (repository/DAO, event store, subscriptions, broker, cache) and per adapter, the ordering, delivery, consistency, and isolation Protean promises — the normative contract that conformance and property tests cite, including the READ COMMITTED floor the optimistic-concurrency check assumes and the bounded no-skip guarantee for `$all` subscriptions.

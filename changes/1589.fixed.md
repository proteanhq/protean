The docs now name `IncorrectUsageError` as the exception an event-sourced aggregate raises when an event has no `@apply` handler. Four pages said `NotImplementedError`, which the framework never raises on this path.

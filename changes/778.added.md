Add three info-level naming-convention diagnostics to the IR builder: `EVENT_NOT_PAST_TENSE` flags domain events named as gerunds (`OrderPlacing`), `COMMAND_NOT_IMPERATIVE` flags commands that do not begin with an imperative verb (`OrderCreation`), and `AGGREGATE_NOT_NOUN` flags aggregates named as verbs or gerunds (`OrderProcessing`). All three are advisory (`level: "info"`), so they never gate CI under the default `[lint].level = "warn"` floor, and they pass through the existing per-element `suppress_checks` / `[lint].suppressions` channels.

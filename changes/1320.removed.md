Remove the `logging.toml` file that `protean new` generated at the project root. Nothing in the framework read it; `[logging]` in `domain.toml` is the one config Protean loads (see `Domain.configure_logging`).

Each Markdown slice of `protean docs generate --type=event-model` now leads with a Given-When-Then before its diagram, so the model reads like an acceptance test. Given is the aggregate, When lists the cluster's commands, and Then lists its non-fact events, all derived structurally from the IR. The `--format=mermaid` output is unchanged: GWT is a Markdown-only lead.

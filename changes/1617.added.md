`protean test` runs the CORE suite on pytest-xdist, one worker per logical CPU, keeping each test file on one worker. `-n/--workers` (or `PROTEAN_TEST_WORKERS`) sets the count, and `0` runs the suite in one process. The FULL, COVERAGE and per-adapter categories are unchanged.

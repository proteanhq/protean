On an event-sourced aggregate, `raise_()` no longer keeps an event when its `@apply` handler, an invariant check or an event enricher raises. The pending events, `_version` and `_event_position` go back to their values from before the call, and the error is re-raised. Field changes the handler made are not undone.

On an event-sourced aggregate, `raise_()` no longer keeps an event whose `@apply` handler or invariant check raised. The pending events, `_version` and `_event_position` go back to their values from before the call, and the error still reaches the caller. Field changes the handler made before it raised are not undone.

Fields accept a `renamed_from=` option (an old name or a list of aliases). A stored payload written under the old key deserializes into the renamed field without an upcaster, the rename is emitted into the IR, and `protean ir diff` reports a single safe `field_renamed` change instead of a breaking remove+add.

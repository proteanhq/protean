`protean check --format sarif` now emits `helpUri`/`informationUri` links to the live documentation domain (`docs.proteanhq.com`) instead of the retired `docs.protean.io`, so the rule and tool links resolve in GitHub Code Scanning and IDE problem panels.

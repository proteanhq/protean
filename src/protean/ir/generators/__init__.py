"""Architecture documentation generators powered by the IR.

Generators consume an IR dict (from ``IRBuilder.build()`` or a JSON file)
and produce human-readable documentation artefacts — Mermaid diagrams,
Markdown tables, event catalogs, and similar.

Shared helpers live in :mod:`protean.ir.generators.base`.
"""

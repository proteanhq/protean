# FieldSpec

The internal field declaration carrier that all field factories (`String()`,
`Integer()`, etc.) produce. A `FieldSpec` holds the field type, constraints,
and configuration until the element class is finalized.

::: protean.fields.spec.FieldSpec
    options:
      show_root_heading: false

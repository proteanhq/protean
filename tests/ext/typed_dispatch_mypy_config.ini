; Strict mypy config for the typed-dispatch fixture run, WITH the plugin.
; Explicitly passed via --config-file so this test does NOT inherit the
; project's pyproject [tool.mypy] (quarantine, module overrides, etc.). This
; leg proves the generic query base composes with the plugin under strict mode.
[mypy]
plugins = protean.ext.mypy_plugin
strict = true

; Strict mypy config for the typed-dispatch fixture run, WITHOUT the plugin.
; Typed dispatch is plain typing, so it must resolve with no plugin at all.
; ADR-0043 is explicit about that, and this leg is what holds it to it.
[mypy]
strict = true

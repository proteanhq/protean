; Minimal, isolated mypy config for the plugin fixture runs.
; Explicitly passed via --config-file so these tests do NOT inherit the
; project's pyproject [tool.mypy] (strict = true, quarantine, etc.) — they
; verify plugin behaviour (reveal_type, base injection) on minimal fixtures,
; not strict compliance.
[mypy]
plugins = protean.ext.mypy_plugin

[DATABASES.secondary]
PROVIDER = "protean.adapters.repository.sqlalchemy.SAProvider"
DATABASE = "SQLITE"
DATABASE_URI = "${SQLITE_DB_LOCATION}"

[CUSTOM]
FOO_USER = "${DB_USER}"
FOO_PASSWORD = "${DB_PASSWORD}"